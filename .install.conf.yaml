- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    # ~/.zsh: zsh
    # ~/.emacs: emacs
    ~/.gitfiles: gitfiles/,
    ~/.gitconfig: gitfiles/gitconfig,

- shell:
    - git update-submodules # after linking ~/.gitconfig
